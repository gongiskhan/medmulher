backend:
  name: git-gateway
  branch: master

media_folder: "images"

collections:
  - label: "Pages"
    name: "pages"
    files:
      - label: "Home Page"
        name: "index"
        file: "index.html"
        fields:
          - {label: Title, name: title, widget: string}
